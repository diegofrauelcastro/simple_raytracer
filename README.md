# simple_raytracer
Personal project with the goal of making a raytracing renderer. The first approach will be CPU based, and future expansions could lead to adapting it to be used in rendering APIs such as OpenGL or Vulkan, and seek optimizations.
